# Root .env — loaded by docker-compose automatically
# Create a copy of this file named exactly ".env" and fill in your keys

# Required: Groq API key (free at console.groq.com)
# Used by: Supermemory Local (memory extraction LLM) + UI Agent Chat
GROQ_API_KEY=gsk_your_groq_key_here
